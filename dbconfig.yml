development:
  dialect: mysql
  datasource: "root:secret@tcp(localhost:33060)/event?parseTime=true&multiStatements=true"
  dir: migrations
  table: schema_migrations
production:
  dialect: mysql
  datasource: ${DATABASE_URL}
  dir: migrations
  table: schema_migrations
